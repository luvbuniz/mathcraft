--- a/index.html
+++ b/index.html
@@ -285,10 +285,14 @@
   </div>
 
   <div class="menuSection">
-    <h2>2️⃣ Pick your Gloomy timer</h2>
+    <h2>2️⃣ Choose your adventure</h2>
     <div class="chooseBtn modeBtn selected" style="cursor:default">
       🏡 MathCraft Adventure
-      <small>Build, shop, climb trees, and earn defenses in one world. When the timer ends, the Gloomies arrive — keep building or fight back!</small>
+      <small>Build, shop, climb trees, and solve math in one world. You decide whether Gloomies can visit.</small>
+    </div>
+    <div class="btnRow" id="gloomyRow" style="margin-top:12px">
+      <button class="chooseBtn selected" data-gloomies="on">💜 Gloomies On</button>
+      <button class="chooseBtn" data-gloomies="off">🌈 No Gloomies</button>
     </div>
     <div id="timeRow" style="margin-top:12px">
       <span style="font-weight:bold; color:#4a6fb5">⏱️ Calm building time:</span>
@@ -924,6 +928,7 @@
 /* ============================ GAME STATE ============================ */
 const G = {
   grade: 2, mode: 'adventure', avatarIndex: 0, playerName: 'Player',
+  gloomiesEnabled: true,
   blocks: 0, powerBlocks: 0, doors: 0, coins: 0, berries: 0, solved: 0, balls: 0,
   furniture: { chair: 0, table: 0, bed: 0, lamp: 0, fence: 0, flower: 0, stove: 0, sink: 0, bathtub: 0, shower: 0, garden: 0, bush: 0, food: 0 },  // placeable backpack items
   running: false, over: false,
@@ -1135,7 +1140,7 @@
   if (!G.playerName) return;
   try {
     localStorage.setItem(saveKey(G.playerName), JSON.stringify({
-      name: G.playerName, grade: G.grade, mode: G.mode, avatarIndex: G.avatarIndex,
+      name: G.playerName, grade: G.grade, mode: G.mode, avatarIndex: G.avatarIndex, gloomiesEnabled: G.gloomiesEnabled,
       blocks: G.blocks, powerBlocks: G.powerBlocks, doors: G.doors, coins: G.coins, berries: G.berries, solved: G.solved,
       furniture: G.furniture, owned: G.owned, timer: G.timer, timerMax: G.timerMax, balls: G.balls,
       shelterRewarded: G.shelterRewarded,
@@ -1165,6 +1170,7 @@
   resetGameState();
   G.playerName = s.name || 'Player';
   G.grade = s.grade; G.mode = 'adventure'; G.avatarIndex = s.avatarIndex;
+  G.gloomiesEnabled = s.gloomiesEnabled !== false;
   G.blocks = s.blocks || 0; G.powerBlocks = s.powerBlocks || 0; G.doors = s.doors || 0; G.coins = s.coins || 0;
   G.berries = s.berries || 0; G.solved = s.solved || 0;
   Object.assign(G.furniture, s.furniture || {});
@@ -1231,6 +1237,7 @@
   resetWorld();
   refreshSavedList();
   $('nameInput').value = G.playerName;
+  syncGloomyMenu();
   $('startScreen').style.display = 'flex';
 }
 
@@ -1909,6 +1916,7 @@
 const ballGeo = new THREE.SphereGeometry(0.18, 10, 10);
 const ballMat = new THREE.MeshLambertMaterial({ color: 0xFFFFFF, emissive: 0x444444 });
 function shootBall() {
+  if (!G.gloomiesEnabled) { toast('🌈 The softball shooter is only needed in a Gloomies On game.', 1900); return; }
   if (!G.doomsday) { toast('🎯 Save your softballs — the Gloomies have not arrived yet!', 1800); return; }
   if (G.balls < 1) { toast('⚽ No softballs! Solve math and choose ⚽ softballs to load up.', 2200); return; }
   G.balls--; refreshHUD();
@@ -2062,7 +2070,7 @@
     wrap.appendChild(mk('wood', '🧱 +' + reward + ' build blocks', () => { G.blocks += reward; G.blockColor = 0xC9B79A; selectSlot('block', true); bumpHud('blockCount'); }));
     wrap.appendChild(mk('power', '⚡ +' + Math.max(2, Math.round(reward * 0.6)) + ' Power Blocks', () => { G.powerBlocks += Math.max(2, Math.round(reward * 0.6)); selectSlot('power', true); Sound.powerZap(); bumpHud('powerCount'); }));
     wrap.appendChild(mk('coins', '🪙 +' + reward + ' coins', () => { G.coins += reward; Sound.coin(); bumpHud('coinCount'); }));
-    wrap.appendChild(mk('balls', '⚽ +' + balls + ' softballs', () => { G.balls += balls; Sound.pop(); }));
+    if (G.gloomiesEnabled) wrap.appendChild(mk('balls', '⚽ +' + balls + ' softballs', () => { G.balls += balls; Sound.pop(); }));
     return;                               // wait for the player's choice (no auto-close)
   } else {
     Sound.wrong();
@@ -2624,6 +2632,11 @@
 }
 
 /* ============================ START MENU ============================ */
+function syncGloomyMenu() {
+  $('gloomyRow').querySelectorAll('button').forEach(b => b.classList.toggle('selected',
+    (b.dataset.gloomies === 'on') === G.gloomiesEnabled));
+  $('timeRow').style.display = G.gloomiesEnabled ? 'block' : 'none';
+}
 function buildMenu() {
   // grade buttons
   $('gradeRow').querySelectorAll('button').forEach(b => b.onclick = () => {
@@ -2631,6 +2644,13 @@
     $('gradeRow').querySelectorAll('button').forEach(x => x.classList.remove('selected'));
     b.classList.add('selected');
     G.grade = parseInt(b.dataset.grade);
+  });
+  $('gloomyRow').querySelectorAll('button').forEach(b => b.onclick = () => {
+    Sound.click();
+    $('gloomyRow').querySelectorAll('button').forEach(x => x.classList.remove('selected'));
+    b.classList.add('selected');
+    G.gloomiesEnabled = b.dataset.gloomies === 'on';
+    syncGloomyMenu();
   });
   $('timeRow').querySelectorAll('.timeBtn').forEach(b => b.onclick = () => {
     Sound.click();
@@ -2689,7 +2709,8 @@
     row.className = 'saveRow';
     const load = document.createElement('button');
     load.className = 'loadBtn';
-    load.innerHTML = '▶ <b>' + (s.name || 'Player') + '</b> — 🧱' + (s.blocks || 0) + ' ⚡' + (s.powerBlocks || 0) + ' 🪙' + (s.coins || 0) + ' • Adventure';
+    const gloomyLabel = s.gloomiesEnabled === false ? '🌈 No Gloomies' : '💜 Gloomies On';
+    load.innerHTML = '▶ <b>' + (s.name || 'Player') + '</b> — 🧱' + (s.blocks || 0) + ' ⚡' + (s.powerBlocks || 0) + ' 🪙' + (s.coins || 0) + ' • ' + gloomyLabel;
     load.onclick = () => { Sound.click(); restoreGame(s); };
     const del = document.createElement('button');
     del.className = 'delBtn';
@@ -2738,16 +2759,20 @@
   $('berryRow').style.display = 'block';        // trees & berries exist in every mode now
   $('powerRow').style.display = 'block';
   $('solvedRow').style.display = 'block';
-  $('ballRow').style.display = 'block';
-  $('hudTimer').style.display = 'block';
-  $('timerBar').style.display = 'block';
-  $('hudShelter').style.display = 'block';
-  if (isTouch) $('btnShoot').style.display = G.doomsday ? 'block' : 'none';
+  $('ballRow').style.display = G.gloomiesEnabled ? 'block' : 'none';
+  $('hudTimer').style.display = G.gloomiesEnabled ? 'block' : 'none';
+  $('timerBar').style.display = G.gloomiesEnabled ? 'block' : 'none';
+  $('hudShelter').style.display = G.gloomiesEnabled ? 'block' : 'none';
+  if (isTouch) $('btnShoot').style.display = G.gloomiesEnabled && G.doomsday ? 'block' : 'none';
   spawnBerries();
   G.running = true;
 
-  $('modeLabel').textContent = '🏡 MathCraft Adventure • Grade ' + G.grade;
-  toast('🏡 Build and shop while it is calm. Solve math for 🧱 blocks, ⚡ Power Blocks, 🪙 coins, or ⚽ ammo before the Gloomies arrive!', 6500);
+  $('modeLabel').textContent = (G.gloomiesEnabled ? '💜 Gloomies On' : '🌈 No Gloomies') + ' • Grade ' + G.grade;
+  if (G.gloomiesEnabled) {
+    toast('🏡 Build and shop while it is calm. Solve math for 🧱 blocks, ⚡ Power Blocks, 🪙 coins, or ⚽ ammo before the Gloomies arrive!', 6500);
+  } else {
+    toast('🌈 No Gloomies! Build, shop, explore, and solve math for as long as you like.', 5200);
+  }
   refreshHUD();
 }
 
@@ -2770,6 +2795,7 @@
   $('endScreen').style.display = 'flex';
 }
 function doomsdayStart() {
+  if (!G.gloomiesEnabled) return;
   G.doomsday = true;
   Sound.sad();
   if (isTouch) $('btnShoot').style.display = 'block';
@@ -2833,7 +2859,7 @@
   }
 
   // Combined adventure: build and shop first, then defend the same world.
-  if (!G.over) {
+  if (G.gloomiesEnabled && !G.over) {
     if (!G.doomsday) {
       G.timer -= dt;
       if (G.timer <= 0) { G.timer = 0; doomsdayStart(); }
